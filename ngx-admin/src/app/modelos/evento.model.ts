export class Evento {
    _id?:string; // Identificador único del evento
    ubicacion?:string;  // Ubicación del evento 
    fecha?:string; // Fecha del evento
    tipoDeEvento?:string; // Tipo de evento
    costoEntrada?:string; // Costo de la entrada al evento
}
